/** @type {import('next').NextConfig} */
const nextConfig = {

    images:{
        domains: ['https://parasteh.com/','https://cloud.parasteh.com','https://cloud.parasteh.com'],
    }
};

export default nextConfig;
